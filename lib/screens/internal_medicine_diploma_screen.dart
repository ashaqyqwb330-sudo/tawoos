import 'package:flutter/material.dart';
import 'program_detail_screen.dart';

class InternalMedicineDiplomaScreen extends StatelessWidget {
  const InternalMedicineDiplomaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ProgramDetailScreen(programId: 'internal_medicine_diploma');
  }
}
