import 'package:flutter/material.dart';
import 'program_chat_screen.dart';

class InternalMedicineDiplomaChatScreen extends StatelessWidget {
  const InternalMedicineDiplomaChatScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ProgramChatScreen(programId: 'internal_medicine_diploma');
  }
}
